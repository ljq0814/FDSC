% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.r
\name{evaluation_fun}
\alias{evaluation_fun}
\title{evaluation_fun}
\usage{
evaluation_fun(
  group_id,
  group_id_hat,
  K,
  K_hat,
  fdy,
  mu_true,
  is_outlier = FALSE,
  out_true = NULL,
  out_set_final = NULL
)
}
\arguments{
\item{group_id}{list of true cluster memberships}

\item{group_id_hat}{list of estimated cluster memberships}

\item{K}{number of true clusters}

\item{K_hat}{estimated number of groups}

\item{fdy}{n x T matrix of functional data}

\item{mu_true}{n x T matrix of true mean functions}

\item{is_outlier}{logical; if TRUE, compute outlier-related metrics (TPR, FPR) in addition to RI and MSE}

\item{out_true}{integer vector of indices of true outliers; required if is_outlier = TRUE}

\item{out_set_final}{integer vector of indices of detected outliers; required if is_outlier = TRUE
Output:}
}
\value{
RI adjusted Rand index

MSE mean squared error

TPR true positive rate for outlier detection (only if is_outlier = TRUE)

FPR false positive rate for outlier detection (only if is_outlier = TRUE)
}
\description{
Evaluates clustering performance under two scenarios: with and without outlier detection.
When \code{is_outlier = FALSE}, the function computes the Adjusted Rand Index (ARI) for clustering accuracy 
and the Mean Squared Error (MSE) for estimation accuracy of the mean functions.  
When \code{is_outlier = TRUE}, the function additionally computes the True Positive Rate (TPR) 
and False Positive Rate (FPR) to assess the accuracy of outlier identification, 
where TPR is the proportion of correctly detected outliers among all true outliers, 
and FPR is the proportion of falsely detected outliers among all non-outliers.
Input:
}
