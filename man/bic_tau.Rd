% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FDSC-MST.R
\name{bic_tau}
\alias{bic_tau}
\title{BIC-Based Selection of Regularization Parameter}
\usage{
bic_tau(
  Y,
  grid,
  B_basis,
  mst_mat,
  gamma_old,
  vl_old,
  zeta_old,
  edges,
  rho = 1,
  a = 3,
  lambda = 0.1,
  tol = 0.1,
  max_iter = 10000,
  tau_lower = 20,
  tau_upper = 30,
  tau_step = 1
)
}
\arguments{
\item{Y}{A numeric matrix of dimension \code{n x T}, representing
functional data for \code{n} observations evaluated at \code{T} time
points.}

\item{grid}{A numeric vector of time points of length \code{T}.}

\item{B_basis}{A numeric matrix of dimension \code{T x n_basis} giving the
B-spline basis evaluated at the time points in \code{grid}.}

\item{mst_mat}{An \code{(n-1) x n} matrix derived from the constructed MST.}

\item{gamma_old}{Initial value of the spline coefficient vector \eqn{\gamma}.}

\item{vl_old}{Initial value of the auxiliary variable \eqn{v_l}.}

\item{zeta_old}{Initial value of the dual variable \eqn{\zeta}.}

\item{edges}{An \code{(n-1) x 2} integer matrix giving the edge list of
the constructed MST.}

\item{rho}{A positive numeric scalar specifying the ADMM learning rate.
Defaults to \code{1}.}

\item{a}{A positive numeric scalar specifying the concavity parameter of
the MCP penalty. Defaults to \code{3}.}

\item{lambda}{A positive numeric scalar specifying the smoothing penalty
parameter, fixed during the search over \eqn{\tau}. Defaults to
\code{0.1}.}

\item{tol}{A positive numeric scalar specifying the ADMM stopping tolerance.
Defaults to \code{0.1}.}

\item{max_iter}{A positive integer specifying the maximum number of ADMM
iterations. Defaults to \code{10000}.}

\item{tau_lower}{A positive numeric scalar specifying the lower bound of the
search range for \eqn{\tau}. Defaults to \code{20}.}

\item{tau_upper}{A positive numeric scalar specifying the upper bound of the
search range for \eqn{\tau}. Defaults to \code{30}.}

\item{tau_step}{A positive numeric scalar specifying the step size of the
search grid for \eqn{\tau}. Defaults to \code{1}.}
}
\value{
A numeric scalar giving the optimal regularization parameter
  \eqn{\tau} selected by BIC.
}
\description{
Selects the optimal regularization parameter \eqn{\tau} for FDSC-MST by
minimizing a BIC criterion over a grid of candidate values, with
\eqn{\lambda} fixed at the provided value.
}
