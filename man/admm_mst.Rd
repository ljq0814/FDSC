% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FDSC-MST.R
\name{admm_mst}
\alias{admm_mst}
\title{ADMM Estimation for FDSC-MST}
\usage{
admm_mst(
  Y,
  grid,
  B,
  A,
  diff_mat,
  mst_mat,
  gamma_old,
  vl_old,
  zeta_old,
  rho = 1,
  a = 3,
  lambda = 0.1,
  tau = 20,
  tol = 0.1,
  max_iter = 10000
)
}
\arguments{
\item{Y}{A numeric matrix of dimension \code{n x T}, representing
functional data for \code{n} observations evaluated at \code{T} time
points.}

\item{grid}{A numeric vector of time points of length \code{T}.}

\item{B}{A numeric matrix equal to \code{kronecker(diag(1, n), B_basis)},
representing the B-spline basis expanded for all observations.}

\item{A}{A numeric matrix equal to \code{kronecker(mst_mat, diag(p))}.}

\item{diff_mat}{The second-order difference penalty matrix.}

\item{mst_mat}{An \code{(n-1) x n} matrix derived from the constructed MST.}

\item{gamma_old}{Initial value of the spline coefficient vector
\eqn{\gamma}.}

\item{vl_old}{Initial value of the auxiliary variable \eqn{v_l}.}

\item{zeta_old}{Initial value of the dual variable \eqn{\zeta}.}

\item{rho}{A positive numeric scalar specifying the ADMM learning rate.
Defaults to \code{1}.}

\item{a}{A positive numeric scalar specifying the concavity parameter of
the MCP penalty. Defaults to \code{3}.}

\item{lambda}{A positive numeric scalar specifying the smoothing penalty
parameter for the B-spline roughness penalty. Defaults to \code{0.1}.}

\item{tau}{A positive numeric scalar specifying the regularization parameter
of the MCP penalty. Defaults to \code{20}.}

\item{tol}{A positive numeric scalar specifying the ADMM stopping tolerance.
Defaults to \code{0.1}.}

\item{max_iter}{A positive integer specifying the maximum number of ADMM
iterations. Defaults to \code{10000}.}
}
\value{
A list with two components:
  \describe{
    \item{gamma}{Estimated spline coefficient vector \eqn{\gamma} at
      convergence.}
    \item{vl}{Estimated auxiliary variable \eqn{v_l} at convergence.}
  }
}
\description{
Solves the ADMM optimization problem for functional data subgroup clustering
via minimum spanning tree, iteratively updating the spline coefficient vector
\eqn{\gamma}, the auxiliary variable \eqn{v_l}, and the dual variable
\eqn{\zeta} until convergence or the maximum number of iterations is reached.
}
\keyword{internal}
